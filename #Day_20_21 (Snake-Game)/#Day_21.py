## class inheritances
# The concept of parent/child classses
"""
class child(parent_class):
    def __init__(self)
        super().__init__()  ==> super refers to the upper class which is parent class

    def child_method(self)
        # to add something to parent method
        super().parent_method()
        then do something extra
"""